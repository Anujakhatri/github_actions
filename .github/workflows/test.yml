name: Test Workflow

on:
 push:


jobs:
 hello:
   runs-on: ubuntu-latest


   steps:
     - uses: actions/checkout@v4

     - name: Show Current Directory
       run: pwd

     - name: List Files
       run: ls

     - name: Show Test File
         run: cat test.txt